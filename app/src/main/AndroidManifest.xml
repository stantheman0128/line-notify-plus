<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 通知監聽權限 -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="com.stanslab.linenotify.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />
    <permission
        android:name="com.stanslab.linenotify.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        tools:node="remove" />

    <!-- Android 11+ package visibility: needed only to verify the LINE app label when
         Android replaces sensitive notification contents with a framework placeholder. -->
    <queries>
        <package android:name="jp.naver.line.android" />
        <package android:name="com.linecorp.line" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LineNotify">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LineNotify">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.LineNotify" />

        <activity
            android:name=".ChatManagementActivity"
            android:exported="false"
            android:theme="@style/Theme.LineNotify" />

        <activity
            android:name=".HelpActivity"
            android:exported="false"
            android:theme="@style/Theme.LineNotify" />

        <receiver
            android:name=".service.ReplyRelayReceiver"
            android:exported="false" />

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- LINE 通知監聽服務 -->
        <service
            android:name=".service.LineNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- 選配的 LINE 聊天畫面偵測。只觀察畫面結構，不模擬點擊或輸入。 -->
        <service
            android:name=".service.LineAccessibilityService"
            android:description="@string/accessibility_service_description"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/line_accessibility_service" />
        </service>

    </application>

</manifest>
