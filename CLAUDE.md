# CLAUDE.md — LINE Notify+

本專案的 AI agent 工作守則放在 **[`AGENTS.md`](./AGENTS.md)**（單一正本，Claude Code 與 ChatGPT/Codex 共用）。

**請直接讀 `AGENTS.md`** — 內含：⛔鐵則（versionCode 不可重用、只准 2 個權限、別加無障礙/網路、別碰 keystore）、autonomous agent 守則（在 branch 上做、行為類 bug 需實機驗證）、build 指令、程式碼地圖、已知 bug 指引。

> 為什麼分兩個檔：Claude Code 自動載入 `CLAUDE.md`、ChatGPT/Codex 自動載入 `AGENTS.md`。為避免兩份內容 drift，規則只維護在 `AGENTS.md`，此檔僅作指標。
